server:
  port: 8086
  servlet:
    context-path: /emat/v1
spring:
  application:
    name: emat

  profiles:
    active: prod

  datasource:
    driver-class-name: oracle.jdbc.OracleDriver
    url: jdbc:oracle:thin:@//51.79.165.43:1521/EMAT
    username: EMAT
    password: nNexjMg5vyEf
    hikari:
      maximum-pool-size: 20
      minimum-idle: 5
      connection-timeout: 20000
      idle-timeout: 300000
      auto-commit: true

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false
    properties:
      hibernate:
        format_sql: false

  servlet:
    multipart:
      max-file-size: 50MB
      max-request-size: 50MB

  threads:
    virtual:
      enabled: true

jwt:
  secret: EMATSecretKeyForJwtSigningMustBeAtLeast32BytesLong123!

pii:
  encryption:
    secret-key: QQEXbMtwd/wTfup0HyhW/b+40jdVupAvnYDwE2nRu7E=

security:
  captcha:
    # Backend-managed CAPTCHA for the login endpoint.
    enabled: ${SECURITY_CAPTCHA_ENABLED:true}
    expiry-in-seconds: 300
    length: 5
    image:
      width: 180
      height: 60

logging:
  level:
    root: INFO
    org.springframework.security: INFO
    org.emat: INFO
    org.hibernate.SQL: INFO