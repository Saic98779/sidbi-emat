# Server Configuration
server.port=8086
server.servlet.context-path=/emat/v1

# Spring Boot Application Name
spring.application.name=emat

# Oracle Database Configuration
spring.datasource.url=jdbc:oracle:thin:@//51.210.96.119:1521/XEPDB1
spring.datasource.username=EMAT
spring.datasource.password=Saichaitu98@
spring.datasource.driver-class-name=oracle.jdbc.OracleDriver

# Connection Pool Configuration
spring.datasource.hikari.maximum-pool-size=20
spring.datasource.hikari.minimum-idle=5
spring.datasource.hikari.connection-timeout=20000
spring.datasource.hikari.idle-timeout=300000
spring.datasource.hikari.auto-commit=true

# JPA Configuration
spring.jpa.hibernate.ddl-auto=create
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.format_sql=false
#spring.jpa.properties.hibernate.use_sql_comments=true
#spring.jpa.properties.hibernate.jdbc.batch_size=10
#spring.jpa.properties.hibernate.order_inserts=true
#spring.jpa.properties.hibernate.order_updates=true
#spring.jpa.properties.hibernate.jdbc.fetch_size=100
#spring.jpa.open-in-view=false

# Logging Configuration
logging.level.root=INFO
logging.level.org.springframework.security=INFO
logging.level.org.emat=INFO
logging.level.org.hibernate.SQL=INFO
logging.level.org.hibernate.type.descriptor.sql.BasicBinder=INFO
logging.level.org.hibernate.tool.schema=INFO
logging.level.org.hibernate.orm.boot=INFO

# Application Profile
spring.profiles.active=dev

# Thread Pool Configuration
spring.threads.virtual.enabled=true

# JWT Configuration
jwt.secret=EMATSecretKeyForJwtSigningMustBeAtLeast32BytesLong123!

# File storage base - set to the path you requested. Use forward slashes to avoid escaping in properties files.
# This controls where uploaded files are stored on disk by the FileStorageServiceImpl.
#file.storage.base=C:/Users/Chait/Desktop/EOIFiles
file.storage.base=/home/ubuntu/uploads/emat

# File download base for building file URLs. Change this for local testing if needed.
# Examples:
# file.download.base=/files
# file.download.base=http://localhost:8086/files
file.download.base=/files

# Multipart File Upload Configuration
spring.servlet.multipart.max-file-size=50MB
spring.servlet.multipart.max-request-size=50MB